{
  "name": "C++",
  // Uncomment this to use the uploaded image
  "image": "mntmtt/sourcevaultdev",
  // Uncomment this to switch to local dockerfile build
  // "build": {
  //   dockerfile: "Dockerfile",
  // },

  "workspaceFolder": "/workspace",
  "customizations": {
    "jetbrains": {
      "backend": "CLion",
      "plugins": [
        "org.sonarlint.idea"
      ],
      "settings": {
      }
    }
  }
}
